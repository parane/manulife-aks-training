pipeline {
    agent any
    stages {
        stage('Checkout git') {
            steps {
                checkout scm
            }
        }
        stage('Docker Build') {
            steps {
                sh "az --version"
            }
        }
    }
}